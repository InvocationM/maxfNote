冯诺依曼计算机结构：五个部分
控制器，运算器，存储器，输入设备，输出设备。

存储器： 存储器中的存储单元地址是唯一的，存储器的位宽有设计计算机时对存储单元的编码方式决定
。如果按存储单元的字节编址，则每个存储单元存放一个八位的二进制数
    存放的是二进制的机器码

控制器：
    指令寄存器
    程序计数器
    存储器地址寄存器
    存储器数据寄存器
    指令译码部件：控制电路产生控制信号，在时序脉冲的作用下同步控制各个部件的动作
    控制电路

通用寄存器：cpu运算时的临时数据的存储（通常是R0~R15,64位处理器的情况下）

运算器：
    完成算术运算和逻辑运算。



控制器处理二进制机器码的流程：
    先从指令寄存器中读取出当前需要执行的指令，然后解析指令，然后执行，然后回写（一般是往，存储器数据寄存器中写入。）
    解析指令： 两个字节，第一个字节，前四位是 操作字节。 后四位是通用寄存器地址。
        第二个字节是 存储器地址。




------------------------------------------
x86体系结构
各种寄存器都是32位的，也有八位的，16位的。
增加了R8 到 R15寄存器

---------


