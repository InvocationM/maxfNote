
ctrl+h   ：  打开当前类的结构。（类层次所有的父类，超类层次包括父类接口，子类层次）

ctrl + shift + v  ： 查看粘贴板记录

ctrl + alt + h  :  选中方法，查看方法调用的层级结构

ctrl + b  :  跳到当前方法源码处


git stash changes  ： 将本地更改放置到暂存区
git rebase :             两者的区别的是  ： 生成patch
git reset : 本地commit，还未push到远程的情况下，进行版本回退。
git revert : 本地commit，且提交到远程了。重新生成新版本