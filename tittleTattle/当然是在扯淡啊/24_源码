用到集合的排序。两种排序方式。
1，Collections.sort()
2，集合.stream().sorted(Comparator.comparing(User::getYear));
一是定义了一种 使用了Tim的归并排序算法。 算法颇为复杂。
二是集合的流操作。
集合中有null的话，会直接NPE。


2,流操作
函数式编程，链式流操作

3, 集合的迭代器
fail-fast 快速失败，直接对当前集合进行操作

fail-safe 安全失败，操作的时候是拷贝的新的集合容器，所以无法感知容器是否已经改变。